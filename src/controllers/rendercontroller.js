export const ctrlRender = {
    renderExpressFileupload: (req, res) => {
        res.render("fileupload");
      },
      renderCloudinary: (req, res) => {
        res.render("cloudform");
      },
}
